const arr = [1, 2,3, 4, 5,6];
for (const iterator of arr) {
    console.log(iterator);
}

//it can be applied on the strings as well 
const greeting = "hello ji";
for (const greet of greeting) {
    console.log(`Each char is ${greet}`);
}

